version: '2'

services:
  app:
    environment:
      - ENV=localdev
      - PORT=8000
      - AUTH=SAML_MOCK
    restart: always
    container_name: app-scheduler
    build:
      context: .
    volumes:
      - ./scheduler:/app/scheduler
    ports:
      - "8000:8000"
